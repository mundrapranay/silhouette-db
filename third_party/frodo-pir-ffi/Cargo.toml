[package]
name = "frodo-pir-ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "frodopirffi"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
frodo-pir = { path = "../frodo-pir" }
bincode = "1.3.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cbindgen = "0.26"

